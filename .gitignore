# Build artifacts (regenerated by `make build`)
embed/client/

# Runtime / process residue
*.log

# Compiled binaries (Rust output is symlinked here by `make build`)
coconote
coconote.exe

# Dependency cache
node_modules/

# Editor / OS noise
.DS_Store
*.swp
.vscode/

# Claude Code session artifacts (per global rules)
.claude/

# Rust build artifacts
server-rs/target/

# Embedded client bundle staged for Rust include_dir
server-rs/embed/client/

# Compiled coconote binary staged for Electron bundle (regenerated by `make app`)
electron/binaries/


# Electron build outputs
electron/dist/
electron/node_modules/

# Distribution artifacts
dist/

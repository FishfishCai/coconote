---
id: thcodeeeeeeeeebx
coconote: true
title: test_code
---

# code

inline: `hello`

inline w/ backticks inside: `` `inner` ``

python:

```python
def hello(name):
    return f"hi, {name}"
```

js:

```javascript
const x = 1;
console.log(x);
```

rust:

```rust
fn main() {
    println!("hi");
}
```

go:

```go
package main
func main() { println("hi") }
```

plain (no lang):

```
plain block
multiple lines kept verbatim
```

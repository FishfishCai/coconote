// Graph feature: the relation-graph overlay. GraphOverlay is the only
// public entry (mounted by editor_ui); the SVG renderer and layout
// physics are internal.
export { GraphOverlay } from "./overlay.tsx";

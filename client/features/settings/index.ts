// Settings feature: the settings panel and the appearance hook that
// applies user prefs to the document. Both are mounted by editor_ui; the
// individual sections and the shared setting-row primitives are internal.
export { Settings } from "./panel.tsx";
export { useAppearance } from "./use_appearance.ts";
